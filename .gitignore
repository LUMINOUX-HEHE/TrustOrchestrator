# Trust Orchestrator — generated / untracked artifacts

# secrets and issuance material — NEVER commit
bootstrap.key
node.key
node.cert.json
shard-*.json
*.revoked

# generated by the CLI / Makefile
bin/
reports/
coverage.out
scratch/

# TLC state files
states/
specs/states/