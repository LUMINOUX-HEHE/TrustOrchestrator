SPECIFICATION Spec

CONSTANTS
    Certs = {c1, c2}
    Council = {m1, m2, m3}
    Quorum = 2
    Watchdogs = {w1, w2, w3}
    DetectQuorum = 2
    Auditors = {a1, a2}
    EscalateQuorum = 2
    Identities = {id1, id2}

\* Bound epoch so the monotonic Commit chain terminates the BFS.
CONSTRAINT BoundedEpoch

INVARIANT Safety

CHECK_DEADLOCK FALSE
