\* Gateway.cfg — full-scale config (local evidence). Scale-reduced configs
\* for CI live in GatewayCI.cfg.
SPECIFICATION Spec

CONSTANTS
    Hashes = {h1, h2}
    Quorum = 2
    MaxLen = 2
    MaxEpoch = 2

CONSTRAINT BoundedEpoch

INVARIANT Safety

CHECK_DEADLOCK FALSE
